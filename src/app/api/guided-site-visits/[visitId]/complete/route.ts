import {NextRequest,NextResponse} from "next/server";import {authorizeGuidedSiteVisit} from "@/lib/guided-site-visits/access";import {exactObject,revision,UUID} from "@/lib/guided-site-visits/core";import {createAdminServerClient} from "@/lib/supabase/admin-server";
export async function POST(request:NextRequest,{params}:{params:Promise<{visitId:string}>}){const auth=await authorizeGuidedSiteVisit(request);if(auth.response)return auth.response;const{visitId}=await params;if(!UUID.test(visitId))return NextResponse.json({success:false,error:"Invalid ID."},{status:400});try{const body=exactObject(await request.json(),new Set(["expectedRevision"]));const result=await createAdminServerClient().rpc("complete_guided_site_visit",{requested_auth_user_id:auth.authorization!.authUserId,requested_visit_id:visitId,requested_expected_revision:revision(body.expectedRevision)});if(result.error)throw new Error(result.error.message);const row=(result.data as Record<string,unknown>[])[0];return NextResponse.json({success:row.result_code==="ok",resultCode:row.result_code,nextRevision:row.next_revision,completionOutcome:row.completion_outcome},{status:row.result_code==="ok"?200:["stale_revision","incomplete","not_editable"].includes(String(row.result_code))?409:404});}catch(e){return NextResponse.json({success:false,error:e instanceof Error?e.message:"Completion failed."},{status:e instanceof TypeError?400:500});}}
