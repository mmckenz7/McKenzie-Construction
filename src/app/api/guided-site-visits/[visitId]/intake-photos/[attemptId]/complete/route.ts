import {NextRequest,NextResponse} from "next/server";import {authorizeGuidedSiteVisit} from "@/lib/guided-site-visits/access";import {exactObject,revision,UUID} from "@/lib/guided-site-visits/core";import {createAdminServerClient} from "@/lib/supabase/admin-server";const F=new Set(["expectedRevision"]);
export async function POST(request:NextRequest,{params}:{params:Promise<{visitId:string;attemptId:string}>}){try{const auth=await authorizeGuidedSiteVisit(request);if(auth.response)return auth.response;const{visitId,attemptId}=await params;if(!UUID.test(visitId)||!UUID.test(attemptId))throw new TypeError("Photo ID is invalid.");const body=exactObject(await request.json(),F),db=createAdminServerClient(),attempt=await db.from("guided_site_visit_intake_attempts").select("asset_id,ai_estimator_assets!inner(storage_path)").eq("id",attemptId).eq("visit_id",visitId).eq("company_id",auth.authorization!.companyId).maybeSingle();if(attempt.error||!attempt.data)return NextResponse.json({success:false,error:"Photo not found."},{status:404});const linked=attempt.data as unknown as{ai_estimator_assets:{storage_path:string}},info=await db.storage.from("ai-estimator-private").info(linked.ai_estimator_assets.storage_path);if(info.error||!info.data)return NextResponse.json({success:false,error:"Uploaded photo not found."},{status:409});const result=await db.rpc("confirm_guided_site_visit_intake_member",{requested_auth_user_id:auth.authorization!.authUserId,requested_visit_id:visitId,requested_attempt_id:attemptId,requested_expected_revision:revision(body.expectedRevision),requested_actual_byte_size:info.data.size,requested_storage_mime_type:info.data.contentType?.split(";",1)[0].toLowerCase()??""});if(result.error)throw new Error("Photo completion failed.");const row=(result.data as Record<string,unknown>[])[0];return NextResponse.json({success:row.result_code==="ok",resultCode:row.result_code,nextRevision:row.next_revision},{status:row.result_code==="ok"?200:row.result_code==="stale_revision"?409:422});}catch(e){return NextResponse.json({success:false,error:e instanceof TypeError?e.message:"Photo completion failed."},{status:e instanceof TypeError?400:500});}}
