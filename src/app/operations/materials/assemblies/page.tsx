import { EstimatingAssemblyBuilder } from "@/components/estimating-assembly-builder";

export const dynamic = "force-dynamic";
export const revalidate = 0;

export default function EstimatingAssembliesPage() {
  return <EstimatingAssemblyBuilder />;
}
