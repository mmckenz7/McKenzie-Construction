# Fence Visual Measure

An isolated, local-only 2D fence measurement prototype.

```sh
npm install
npm run validate
npm run dev
```

Open the local URL, choose **Draw**, then tap or click to place connected points. Select a point or span to edit it. Saving uses only this browser's local storage.
